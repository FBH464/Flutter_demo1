import 'package:flutter/material.dart';

class EditarProductos extends StatelessWidget {
  const EditarProductos({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}
